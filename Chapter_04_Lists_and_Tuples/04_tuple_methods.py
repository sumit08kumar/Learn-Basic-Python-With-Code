# Creating a tuple using ()
t = (1, 2, 4, 5, 4, 1, 2, 1, 1)
# print(t)

# Count functions
print(t.count(1))

# Indexing function
print(t.index(4))

# Example

myTuple = (1, 2, 3, 4)
result = myTuple + (5, )
print(result)